# TLI 에이전트 작업 지침

## 적용 범위와 필수 문서

학습 계획, 공부 중 개념 질문, 학습 기록, 면접 준비 자료, 관련 Git 게시 요청을 처리하기 전에 다음 문서를 순서대로 끝까지 읽는다.

1. `docs/study-log-workflow.md`
2. `docs/study-log-verification.md`
3. `docs/study-log-format.md`
4. `docs/study-pr-format.md`

면접 준비용 Markdown 또는 HTML을 다루는 경우 `docs/study-html-format.md`도 끝까지 읽는다.

이 파일은 반드시 적용할 작업 경계와 문서 라우팅을 정의한다. 상세 절차와 형식의 단일 출처는 위 문서들이다.

## 반드시 지킬 작업 경계

- 사용자가 공부 계획이나 시작을 말한 것만으로 `daily/`, `concepts/`, `interviews/` 파일을 만들거나 수정하지 않는다. 대화에서 계획과 시작 시각만 확인한다.
- 사용자가 `오늘 공부 끝`이라고 말하면 학습 기록 정리를 시작하되, 승인 전 초안은 채팅으로만 제시한다.
- 회고는 사용자가 직접 작성한다. 사용자의 명시적인 편집 요청과 교체 승인이 없으면 맞춤법, 띄어쓰기, 문장부호, 줄바꿈, 어휘, 표현 순서를 포함해 원문 그대로 보존한다.
- 기존 파일을 수정할 때는 사용자 원문, 기존 포맷, 추가 섹션을 보존한다. 파일 전체를 임의로 재작성하거나 덮어쓰지 않는다.
- 최종 게시 전에는 작성자와 분리된 실제 서브 에이전트가 `docs/study-log-verification.md`에 따라 읽기 전용으로 검수해야 한다. 검수자는 파일 수정, commit, push, PR 생성을 하지 않는다.
- 서브 에이전트 검수를 수행할 수 없으면 검수했다고 말하지 않으며, 최종 게시를 중단하고 사용자에게 알린다.

## 학습 기록 승인 상태

1. `공부 시작 또는 계획`: 대화에서만 확인한다. 파일을 변경하지 않는다.
2. `오늘 공부 끝`: Daily 초안과 Concepts 후보를 채팅으로 제시하고 사용자의 회고와 선택을 받는다. 파일을 변경하지 않는다.
3. `최종본 준비`: 회고와 사용자의 선택을 반영한 초안을 서브 에이전트가 검수한다. 작성 에이전트가 문제를 반영한 최종본을 채팅으로 다시 제시한다. 파일을 변경하지 않는다.
4. `최종 게시 승인`: 검수된 최신 최종본이 제시된 뒤 사용자가 아래 승인 문구 중 하나를 실제 명령으로 사용한 경우에만 승인 파일을 작성하고 작업 브랜치에 commit과 push를 한 뒤 PR을 생성한다.

최종 게시 승인 문구는 다음과 같다.

- `이대로 Git에 올려줘`
- `최종 Git에 올려줘`
- `완료 Git`
- `Git에 올려줘`
- `Git에 push 해줘`

승인 문구를 질문, 인용, 예시, 규칙 논의 과정에서 언급한 것은 게시 승인으로 처리하지 않는다. 검수된 최신 최종본과 게시할 경로를 가리키는 실제 명령인지 문맥으로 확인한다.

검수된 최신 최종본이 없는 상태에서 `Git에 올려줘`라는 말만 나오면 즉시 게시하지 않는다. `오늘 공부 끝` 단계부터 필요한 초안 작성과 검수를 먼저 진행한다.

## Git 게시 범위

- 최종 게시 승인은 사용자가 확인한 최신 최종본과 그에 명시된 경로에만 적용된다.
- 관련 없는 기존 변경, 다른 사용자의 변경, 승인되지 않은 untracked 파일을 stage하거나 수정하지 않는다.
- 정확한 경로만 `git add`하고, commit 전에 `git status`와 staged diff를 확인한다.
- 검증이 끝난 승인 파일만 commit하고 `docs/study-pr-format.md`에 따른 작업 브랜치를 remote로 push한 뒤 PR을 생성한다. `main`에는 직접 push하지 않는다.
- PR merge와 배포는 위 최종 게시 승인 문구에 포함되지 않는다. 사용자가 각각 별도로 명시적으로 요청한 경우에만 진행한다.
- 샌드박스, 인증, 권한 확인 절차를 우회하지 않는다.

## 지침 문서 자체 변경

이 지침의 승인 전 파일 변경 금지는 학습 산출물인 `daily/`, `concepts/`, `interviews/`에 적용된다. `AGENTS.md`, `docs/`, `README.md` 같은 지침 문서는 사용자가 그 변경을 명시적으로 요청한 경우에만 수정한다.
